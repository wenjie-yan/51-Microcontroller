#include <REGX52.H>
#include "Delay.h"
#include "Nixie.h"
void main()
{
	
	while(1)
	{
		Nixie(1,1);
		Nixie(2,3);
		Nixie(3,1);
		Nixie(4,4);
		Nixie(5,5);
		Nixie(6,2);
		Nixie(7,0);
		Nixie(8,9);
		
	}
}